# GhosttyPet

Tiny macOS desktop pet inspired by Ghostty's clean source animation.

```sh
make -C GhosttyPet
open GhosttyPet/build/GhosttyPet.app
```

Controls: drag to move, right-click or Escape to quit.

Source lives in `GhosttyPet/`. Build output stays ignored.
